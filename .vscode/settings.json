{
    "editor.formatOnSave": true,
    // NOTE: This might need to be adjusted to work with our monorepo setup
    // where we have two separate configurations for the styling for `tools`
    // and `community`.
    "unocss.root": "tools"
}
