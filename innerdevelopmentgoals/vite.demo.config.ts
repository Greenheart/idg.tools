import { defineConfig } from 'vite'

// IDEA: Use environment variables to conditionally use the right vite config.
// Use @sveltejs/package config as default for component development
// Use another config for the Vite library mode, both for building the library and previewing it.
export default defineConfig({})
