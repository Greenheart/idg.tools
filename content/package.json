{
    "name": "content",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "chokidar \"src/**/*.json\" -c \"pnpm --filter content build\" --initial --silent",
        "build": "node scripts/esbuild.js && node scripts/compiled/build-content.js"
    },
    "devDependencies": {
        "chokidar-cli": "^3.0.0",
        "esbuild": "^0.15.12",
        "fast-glob": "^3.2.12",
        "slugify": "^1.6.5",
        "@types/node": "^18.11.9"
    }
}
