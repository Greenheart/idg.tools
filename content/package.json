{
    "name": "content",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "node scripts/compile.js && node scripts/dev.js --",
        "build": "node scripts/compile.js && node scripts/build.js --",
        "colors": "node scripts/compile.js && node scripts/compiled/update-colors.js"
    },
    "devDependencies": {
        "@types/node": "^20.5.0",
        "chokidar": "^3.5.3",
        "esbuild": "~0.19.2",
        "fast-glob": "^3.3.1",
        "lodash.debounce": "^4.0.8",
        "slugify": "^1.6.6"
    }
}
